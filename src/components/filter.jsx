const React = require('react');

class Filter extends React.Component {
    render() {
        return (
            <div className="filter">filtrar por favoritos</div>
        )}
}

module.exports = Filter;